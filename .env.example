# Shared password for the facilitator results page at /results.
# Set this in Railway's Variables tab. The app will not serve results
# without it configured.
RESULTS_PASSWORD=change-me

# Set automatically by Railway when a volume is attached to the service.
# Leave unset locally; the app falls back to a ./data folder.
# RAILWAY_VOLUME_MOUNT_PATH=/data
